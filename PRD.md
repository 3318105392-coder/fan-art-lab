# FanArt Lab — 饭圈物料 DIY 工具 · 产品需求文档（PRD）

---

## 修订记录

| 版本 | 日期 | 作者 | 修订内容 |
|---|---|---|---|
| v1.0 | 2026-07-24 | — | 初稿：项目定位、技术方案、制品规格 |
| v1.1 | 2026-07-25 | — | 更新已上线功能、修正贴纸定位、补充部署方案 |

---

## 一、全局说明

### 1.1 名词解释

| 术语 | 解释 |
|---|---|
| **出血线** | 印刷裁切参考线，位于成品尺寸外 1.5mm 处，导出时自动隐藏，仅编辑器内可见（红色虚线） |
| **安全区** | 出血线以内的区域，即最终成品尺寸（小卡 54×86mm） |
| **图层** | 画布上每个独立元素（图片/文字/贴纸）为一个图层，支持独立拖拽、缩放、旋转 |
| **300 DPI** | 印刷级分辨率，确保下载图片清晰度满足线下打印需求 |
| **正反面** | 小卡和拍立得均支持正反两面独立编辑，通过 Tab 一键切换，两面图层互不影响 |
| **模版** | 预设的拍立得边框背景图，应用后固定为底层不可移动 |

### 1.2 统一规则

- 无登录/注册机制，所有数据仅存于浏览器内存，关闭页面即清空
- 列表默认为空，用户添加内容后按时间倒序展示
- 网络异常：纯前端应用，首次加载后无需网络（模板/贴纸图片除外，404 时静默隐藏）
- 导出格式：PNG，300 DPI，文件名自动标注正/背面

---

## 二、项目背景

### 2.1 现状

饭圈粉丝在应援季、偶像生日等节点有大量物料制作需求（小卡、拍立得等）。当前解决方案存在三大痛点：

- **工具割裂**：需要 PS / Canva / 手机修图 App / 淘宝排版店等多个工具切换
- **门槛过高**：PS 学习成本高，通用工具缺乏饭圈垂类模板，需要从零设计
- **成本不低**：找人代做小卡单张 5-20 元不等，淘宝店铺沟通成本高

### 2.2 方案

提供一站式免费 Web 工具，聚焦饭圈物料垂直场景，用户上传照片后通过拖拽、贴纸、文字即可完成设计，300 DPI 高清导出直接打印。借助 AI 编程工具（Claude Code），非技术背景独立完成从 PRD 到开发部署的全流程。

### 2.3 目标

- 降低饭圈物料制作门槛，从「需要 PS 技能」降为「浏览器拖拽即可」
- 免费替代付费代做服务，单次编辑到导出控制在 3 分钟内
- 作为 AI 辅助产品开发的完整案例，验证非技术背景产品人的落地能力

---

## 三、项目范围

### 3.1 产品架构图

```
FanArt Lab
├── 首页
│   ├── Hero 入口（拖拽上传 → 直接进入小卡编辑）
│   └── 制品类型卡片（小卡 / 拍立得 / 贴纸 / 明信片）
│
├── 小卡编辑器 ←── 当前核心功能
│   ├── 顶部：正/反面 Tab 切换
│   ├── 左侧面板
│   │   ├── 效果图 Tab（尺寸信息 / 背景色 / 出血线 / 网格 / 缩放 / 正反面预览）
│   │   ├── 模版 Tab（49 套通用模版）
│   │   ├── 贴纸素材 Tab（Emoji + 星星 / 爱心 / 猫爪 / 蝴蝶结 / 音符）
│   │   └── 文字 Tab（字体 hover 预览 / 粗细 / 斜体 / 字号 / 颜色 / 对齐 / 透明度）
│   ├── 画布区（Konva Canvas，拖拽编辑）
│   └── 右侧面板
│       ├── 图层列表（拖拽排序 / 显隐 / 锁定 / 删除）
│       └── 操作按钮（上传图片 / 添加文字 / 下载 PNG）
│
├── 拍立得编辑器 ←── 当前核心功能
│   ├── 顶部：正/反面 Tab 切换
│   ├── 正面额外：边框宽度 / 边框颜色 / 底部白边比例 / 移除模版
│   ├── 画布区（含拍立得四边边框渲染 + 照片区 + 白边区）
│   └── 其余同小卡编辑器
│
├── 贴纸编辑器（占位页）
└── 明信片编辑器（占位页）
```

### 3.2 功能模块清单

| 模块 | 功能点 | 状态 |
|---|---|---|
| 画布编辑 | 图片上传（拖拽/点击）、拖拽移动、8 锚点缩放、旋转 | ✅ |
| 图层管理 | 多图层显隐/锁定/排序/删除、拖拽重排序、Delete 键删除 | ✅ |
| 图层命名 | 按来源自动命名（Emoji/星星/文字/模版等）+ 同名编号 | ✅ |
| 文字系统 | 15 款字体、hover 预览、3 档粗细、斜体、字号/颜色/对齐/透明度 | ✅ |
| 背景系统 | 13 色预设 + 自定义取色器 + 透明选项 + 全画布覆盖 | ✅ |
| 模版系统 | 49 套 PNG 透明底模板、全画布固定背景、点击即时切换 | ✅ |
| 贴纸素材 | Emoji 40 个 + 5 类图片贴纸 127 张、分类 Tab 组织 | ✅ |
| 导出系统 | 300 DPI PNG、自动隐藏出血线和变换框、文件名标注正/反面 | ✅ |
| 正反面 | 独立图层栈、Tab 一键切换、切面时抓拍缩略图预览 | ✅ |
| 拍立得边框 | 四边框架独立渲染、宽度/颜色/白边比例可调、与模板互斥 | ✅ |
| 部署运维 | Cloudflare Pages 自动部署、模板图片 119MB→44MB 压缩 | ✅ |

---

## 四、业务流程

### 4.1 核心业务流程图（小卡制作为例）

```
用户                  前端                    系统
│                     │                      │
├─ 打开首页            │                      │
│  ──────────────→  加载页面                 │
│                     │                      │
├─ 点击「小卡」或      │                      │
│  拖拽照片到 Hero     │                      │
│  ──────────────→  路由跳转                │
│                     │ ──────────────→    初始化画布（54×86mm）
│                     │                      │
├─ 上传图片            │                      │
│  ──────────────→  FileReader 读取          │
│                     │ ──────────────→    创建图片图层
│                     │                      │
├─ 拖拽/缩放/旋转      │                      │
│  ──────────────→  Konva 实时渲染           │
│                     │                      │
├─ 添加文字/贴纸       │                      │
│  ──────────────→  创建新图层               │
│                     │                      │
├─ 切换字体/颜色       │                      │
│  ──────────────→  更新图层属性             │
│                     │                      │
├─ 调整图层顺序        │                      │
│  ──────────────→  MOVE_LAYER_TO           │
│                     │                      │
├─ 切换「背面」Tab     │                      │
│  ──────────────→  保存正面预览 → 切换背面   │
│                     │                      │
├─ 编辑背面（同上）     │                      │
│                     │                      │
├─ 点击「下载 PNG」    │                      │
│  ──────────────→  隐藏出血线+变换框        │
│                     │ ──────────────→    toDataURL(300 DPI)
│                     │                      │
│   ← 浏览器下载 ←──  生成 Blob 链接          │
│                     │                      │
```

### 4.2 拍立得正面编辑流程

```
用户点击模版
  → dispatch SET_TEMPLATE(src)
  → 模板图加载为 full-canvas 固定背景
  → 边框控件隐藏（边框与模板互斥）
  → 用户图层叠加在模板上方

用户调整边框
  → 边框宽度（1-6mm）/ 白边比例（10-45%）滑块
  → PolaroidFrameRender 实时更新四边 Rect + 底部 Rect
  → 照片区渲染背景色

用户切换正/反面
  → 正面图层 → frontState（保留）
  → 背面图层 → backState（独立）
  → 切回时恢复原状态
```

### 4.3 贴纸素材添加流程

```
用户点击贴纸 Tab → 选择分类（Emoji/星星/爱心等）
  → 点击贴纸
  → 图片类：addImageSticker(src, catName)
  → Emoji 类：addEmojiSticker(emoji)
  → dedupName(catName, layers) 计算唯一名称
  → ADD_LAYER 创建图层到画布中心
  → 用户可拖拽/缩放/旋转
```

---

## 五、功能需求（用例描述）

### 5.1 用例：添加文字图层

- **前置条件**：用户已进入小卡或拍立得编辑器
- **触发方式**：右侧面板「添加文字」按钮 / 左侧文字 Tab「添加新文字」按钮
- **基本流程**：
  1. 系统创建名为「文字」（或「文字N」如有同名）的文字图层
  2. 图层默认位置为画布中下方，内容为「双击编辑文字」
  3. 默认字体：Noto Sans SC，字号 18px，颜色 #1e1b4b，居中对齐
  4. 图层自动选中，显示蓝色变换框
- **后置条件**：图层列表新增一项，画布显示文字
- **异常流程**：无

### 5.2 用例：编辑文字内容

- **前置条件**：画布上存在文字图层
- **触发方式**：双击画布上的文字
- **基本流程**：
  1. 被双击的 Konva 文字设为 opacity: 0（不可见但保留尺寸）
  2. 在文字原位置叠加透明 textarea
  3. textarea 继承图层的字体/字号/颜色/旋转角度
  4. 用户输入时实时同步到图层数据（UPDATE_LAYER）
  5. 点击画布外区域或按 Enter → 退出编辑，恢复 Konva 文字可见
  6. 按 Esc → 取消编辑
- **后置条件**：图层文字内容更新
- **异常流程**：编辑过程中切换正/反面 → 编辑状态自动结束
- **边界说明**：编辑时 Transformer 缩放框保持在文字外围；无右键菜单；无撤销功能

### 5.3 用例：拖拽重排图层

- **前置条件**：画布上存在 ≥2 个图层
- **触发方式**：在图层面板按住图层项拖拽
- **基本流程**：
  1. 拖拽开始时，被拖图层变为半透明虚线框
  2. 拖拽到目标位置时，图层间出现红色横线指示插入位置
  3. 松手 → 图层移动到目标位置（MOVE_LAYER_TO）
  4. 仅支持上下拖拽，不支持水平移动
- **后置条件**：图层列表和画布 z-index 同步更新
- **异常流程**：拖拽到列表外部 → 恢复原位

### 5.4 用例：更换字体

- **前置条件**：已选中文字图层
- **触发方式**：左侧文字 Tab 字体列表
- **基本流程**：
  1. 鼠标在字体列表上移动 → 画布文字即时预览该字体（onMouseEnter）
  2. 鼠标移出字体列表 → 恢复原字体（onMouseLeave，useRef 防竞态）
  3. 点击某字体 → 锁定当前字体，取消预览态
- **后置条件**：图层 fontFamily 属性更新
- **异常流程**：预览中切换正/反面 → 预览自动清除
- **边界说明**：部分装饰字体（马山正楷等）仅支持单一粗细

### 5.5 用例：下载 PNG

- **前置条件**：画布上存在 ≥1 个可见图层
- **触发方式**：右侧「下载 PNG」按钮
- **基本流程**：
  1. 隐藏 Transformer（变换框）
  2. 隐藏 bleed Layer（红色出血线）
  3. stage.toDataURL({ pixelRatio: 3, mimeType: 'image/png' })
  4. 恢复 Transformer 和 bleed Layer 的可见性
  5. 浏览器触发下载，文件名：`小卡_正面_时间戳.png`
- **后置条件**：文件保存到本地
- **异常流程**：stage ref 为空 → 不执行

### 5.6 用例：应用模板

- **前置条件**：进入编辑器（小卡或拍立得）
- **触发方式**：左侧模板 Tab 点击模板缩略图
- **基本流程**：
  1. 移除旧模板图层（name === '模版'）
  2. 创建新的 image 图层，尺寸 = 全画布（含出血区），名称为「模版」（或模版N）
  3. 图层 locked = true（不可拖拽移动）
  4. 模板位于画布最底层，用户内容在上方
- **后置条件**：模板显示于画布背景
- **异常流程**：图片加载失败 → onError 隐藏
- **拍立得特殊规则**：应用模板后边框控件隐藏，模板覆盖拍立得边框区域

---

## 六、数据与埋点规划（待实施）

| 事件 | 触发时机 | 用途 |
|---|---|---|
| page_view | 页面加载 | PV 统计 |
| editor_enter | 进入编辑器 | 各编辑器使用率 |
| layer_add | 添加图层 | 图层类型分布 |
| template_click | 点击模板 | 热门模板排行 |
| export_click | 点击下载 | 导出转化率 |
| side_switch | 切换正反面 | 正反面使用比 |

---

## 七、已知限制与风险

| 类别 | 内容 |
|---|---|
| 设备 | 仅桌面端，移动端未适配 |
| 数据 | 无后端/用户系统，浏览器关闭即丢失所有数据 |
| 功能 | 无撤销/重做（Ctrl+Z） |
| 功能 | 无图片滤镜（亮度/对比度/饱和度） |
| 功能 | 文字编辑仅支持双击 + 透明度叠加，无右键菜单 |
| 功能 | 贴纸制作器（AI 抠图+裁剪+描边导出）未实现 |
| 功能 | 明信片编辑器未实现 |
| 性能 | 127 张贴纸图片首次加载需数秒，后续有缓存 |
| 网络 | 模板/贴纸图片依赖 Cloudflare CDN，国内部分地区可能慢 |
| 安全 | 无用户认证，无法防止恶意使用 |

---

## 八、部署与运维

- **平台**：Cloudflare Pages（免费）
- **域名**：fan-art-lab.pages.dev
- **代码仓库**：github.com/3318105392-coder/fan-art-lab
- **部署方式**：Git push → Cloudflare 自动构建部署（`npm run build` → `dist/`）
- **回滚**：Cloudflare Pages 支持一键回滚到历史版本

---

> 📅 创建：2026-07-24 · 最后更新：2026-07-25
> 📝 版本：v1.1
